import { PRIMARY_COLOR } from '@/styles/constants';
import styled from 'styled-components';

export const HeaderView = styled.h1`
  font-size: 48px;
  background-color: ${PRIMARY_COLOR};
  padding: 20px;
  border-radius: 10px;
`;
